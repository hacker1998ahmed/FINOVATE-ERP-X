# مراجعة جاهزية نظام FINOVATE ERP X ودليل الإعداد والتشغيل

**تاريخ المراجعة:** 4 سبتمبر 2026

**المستودع الذي تمت مراجعته:** [hacker1998ahmed/FINOVATE-ERP-X](https://github.com/hacker1998ahmed/FINOVATE-ERP-X)

## 1. الخلاصة التنفيذية

**الحكم النهائي: النظام غير جاهز حاليًا للاستخدام الإنتاجي أو لتسليمه للمستخدمين.** يمكن الاحتفاظ بملفاته في Google Drive كنسخة أو مستودع ملفات، ولكن لا يمكن استخدام Google Drive نفسه كاستضافة مباشرة لتطبيق HTML/JavaScript يعمل مثل موقع ويب. يجب استخدام استضافة ويب مناسبة للواجهة، مثل GitHub Pages أو Netlify أو Cloud Storage، مع إبقاء Google Sheets وGoogle Apps Script كخدمات خلفية عند إصلاحهما.

المشروع قابل للتحويل إلى نسخة تجريبية عاملة، لكن توجد فجوات جوهرية تمنع اعتباره نظام ERP جاهزًا تجاريًا:

| المجال | نتيجة الفحص | مستوى الخطورة |
|---|---|---|
| ملفات الواجهة | موجودة، ونجح سكربت بناء مجلد `dist/` | متوسط |
| قاعدة البيانات في Drive | ملف `FINOVATE_DB` موجود، لكنه يحتوي على ورقة واحدة فقط باسم `الورقة1` | حرج |
| الربط الخلفي | رابط النشر يستجيب، لكنه يعيد `Sheet not found: undefined` عند الاختبار | حرج |
| توافق الواجهة مع Apps Script | الواجهة تطلب `createRecord` و`findRecords` وغيرهما، بينما `Code.gs` لا يوجّه هذه العمليات في `handleRequest` | حرج |
| تسجيل الدخول | يتم حاليًا من حسابات ثابتة داخل `js/auth.js`، وليس من قاعدة البيانات | حرج |
| كلمات المرور | موجودة نصًا صريحًا داخل JavaScript وملفات التوثيق | حرج |
| الصلاحيات | مطبقة في الواجهة فقط ويمكن تجاوزها، وليست مفروضة في الخادم | حرج |
| النسخ الاحتياطي | واجهة محاكاة؛ دوال التنزيل والاستعادة والحذف تعرض رسائل فقط | حرج |
| الاختبارات | أمر Node ينتهي بنجاح، لكنه لا يمثل اختبارًا متصفحياً أو اختبار تكامل حقيقي | مرتفع |
| استضافة Google Drive | غير مناسبة لاستضافة الموقع مباشرة؛ Drive للتخزين والمشاركة وليس Web Hosting | حرج |

## 2. ما تم التحقق منه فعليًا

تمت مراجعة المستودع المحدد وسحب النسخة الحالية. يحتوي المشروع على 80 ملفًا متتبعًا، و27 ملف JavaScript، و35 ملف ترجمة، وثلاث صفحات HTML، ومجلد Apps Script.

تم تشغيل `deploy.sh` بنجاح، وأنشأ مجلد `dist/` ونسخ ملفات الواجهة إليه. هذا النجاح يثبت أن عملية النسخ والبناء الأساسية تعمل، ولا يثبت أن النظام متصل بقاعدة البيانات أو أن الوحدات التجارية تعمل فعليًا.

تم تشغيل `node tests/test-suite.js` وانتهى برمز نجاح، لكن الملف لا ينفذ اختبارًا متصفحياً متكاملاً، كما أن بعض مفاتيح الجلسة التي يبحث عنها الاختبار (`finovate_session`) لا تطابق المفاتيح المستخدمة في المصادقة (`finovate-user` و`finovate-session`). لذلك لا يصح استخدام هذه النتيجة كاعتماد جودة إنتاجي.

تم فحص ملف Google Sheets الموجود عبر Google Workspace. بياناته الحالية:

- **الاسم:** `FINOVATE_DB`
- **الرابط:** [فتح قاعدة البيانات FINOVATE_DB](https://docs.google.com/spreadsheets/d/1Y_1pXGbUMYXJFYne-5uqM3kYgMhDjrVWoej4yII60mM/edit)
- **المناخ الزمني:** `Africa/Cairo`
- **الأوراق الحالية:** `الورقة1` فقط
- **النتيجة:** لا توجد حاليًا أوراق `Users` أو `Companies` أو بقية جداول النظام.

تم اختبار رابط Apps Script المرفق:

- **الرابط الحالي:** [فتح Web App](https://script.google.com/macros/s/AKfycby-3QG6b4aZNo-k9Z4r15YtYUT2TwQhtOBQRBs3qIEb7H8yd4ofq8OwvG25ZipNzosi/exec)
- **الاستجابة الفعلية:** `{"success":false,"message":"Sheet not found: undefined"}`
- **الاستنتاج:** الرابط ليس اختبارًا ناجحًا لتسجيل الدخول، كما أن الكود الذي يخدمه يبدو مختلفًا عن مسار `apps-script/Code.gs` الموجود في المستودع، أو أنه يستخدم نسخة قديمة من المشروع.

## 3. أهم المشكلات التي يجب إصلاحها قبل التشغيل

### 3.1 Google Drive ليس استضافة للموقع

يمكن رفع ملفات المشروع إلى Drive للحفظ والمشاركة والتنزيل، لكن فتح `index.html` من Drive لا يوفر استضافة ويب صحيحة لملفات CSS وJavaScript وService Worker. كما أن استضافة المواقع في Google Drive تم إيقافها منذ سنوات [1].

**التصميم الصحيح:**

1. استضافة الواجهة على GitHub Pages أو Netlify أو Google Cloud Storage.
2. وضع Google Apps Script كـ Web App للـ API.
3. وضع Google Sheets كقاعدة بيانات أولية.
4. استخدام Google Drive لتخزين المرفقات والنسخ الاحتياطية فقط.

### 3.2 الواجهة والخادم غير متوافقين

ملف `js/api.js` يرسل الطلبات بصيغة مختلفة عن الصيغة التي يتوقعها الكود القديم المرفق في النص المرسل. كما أن `Code.gs` يعرّف دوال `createRecord` و`updateRecord` و`deleteRecord`، لكنه لا يستدعيها داخل `handleRequest`. ولا توجد حالات `findRecords` أو `createRecord` أو `updateRecord` أو `deleteRecord` في `switch` الرئيسي.

بالنتيجة، العمليات التجارية لن تصل إلى الدوال المناسبة حتى لو تم إنشاء أوراق Sheets.

### 3.3 تسجيل الدخول غير موصول بقاعدة البيانات

`js/auth.js` يحتوي على ثلاثة مستخدمين ثابتين داخل المتصفح:

- `admin@finovate.com / admin123`
- `accountant@finovate.com / account123`
- `manager@finovate.com / manager123`

هذا يعني أن تسجيل الدخول قد يعمل حتى دون وجود Backend، وأن أي شخص يستطيع رؤية بيانات الدخول من المصدر البرمجي. يجب حذف هذه الحسابات من الواجهة قبل أي نشر عام، وتنفيذ المصادقة في Apps Script أو خدمة هوية مناسبة.

### 3.4 لا يجوز تخزين كلمات المرور كنص صريح

`Code.gs` يقارن كلمة المرور الواردة مباشرة بقيمة العمود `password`. هذا غير آمن. يجب تخزين كلمة المرور على شكل hash مع salt، أو استخدام Google Identity/OAuth أو مزود هوية متخصص. يجب كذلك إلغاء كلمات المرور التجريبية بعد الإصلاح.

### 3.5 الصلاحيات الحالية ليست حماية حقيقية

`js/auth.js` يحدد صلاحيات الأدوار في JavaScript. أي مستخدم يستطيع تعديل JavaScript أو إرسال طلب مباشر إلى Apps Script. يجب فرض التحقق من هوية المستخدم وصلاحياته على الخادم في كل عملية قراءة أو إنشاء أو تعديل أو حذف.

### 3.6 النسخ الاحتياطي غير منفذ

`js/backup.js` يحتوي على محاكاة؛ إنشاء النسخة يعرض رسالة نجاح دون إنشاء ملف حقيقي، والتنزيل والاستعادة والحذف لا تنفذ عمليات Google Drive. لا يجوز الاعتماد على هذا الجزء لحماية بيانات مالية.

### 3.7 وجود مسارين متعارضين لقاعدة البيانات

`apps-script/Code.gs` يستخدم أعمدة مثل `id`, `email`, `password`, `name`، بينما التوثيق المرفق في النص يقترح أعمدة مختلفة مثل `ID`, `Username`, `Password`, `Role`, `FullName`, `Email`, `IsActive`. يجب اختيار مخطط واحد رسمي، ثم تطبيقه في الواجهة والخادم والـ Sheets والتوثيق.

## 4. خطة الإصلاح الإلزامية قبل النشر

### المرحلة A: تثبيت التصميم

1. اعتماد Google Sheet واحد، وليكن `FINOVATE_DB`.
2. اعتماد أسماء أعمدة موحدة بحروف صغيرة أو كبيرة، وعدم خلط `ID` مع `id`.
3. اعتماد أسماء الأوراق الموجودة في `Database.gs` أو تقليصها إلى الوحدات التي سيتم تشغيلها فعلًا.
4. تحديد ما إذا كان النظام لشركة واحدة أم متعدد الشركات.
5. تحديد العملة والضريبة والصلاحيات وسياسة الاحتفاظ بالبيانات.

### المرحلة B: إصلاح Apps Script

1. ربط الخادم بالـ Spreadsheet ID الصحيح أو جعل مشروع Apps Script تابعًا مباشرة لملف Sheets المقصود.
2. إضافة مسارات API صريحة للعمليات الأساسية.
3. إضافة التحقق من المدخلات ونوع العملية واسم الورقة.
4. إضافة تسجيل تدقيق للخادم.
5. إضافة قفل للعمليات الحساسة ومنع الكتابة المتزامنة غير الآمنة.
6. عدم فتح كل الجداول لأي طلب مجهول.
7. استخدام توكن جلسة منتهي الصلاحية أو Google OAuth بدل إرسال كلمة المرور في كل طلب.
8. عدم إرجاع حقول كلمة المرور في أي استجابة.

**ملاحظة مهمة:** لا يكفي نسخ `Code.gs` و`Database.gs` كما هما؛ يجب دمجهما في نسخة واحدة متسقة ثم اختبارها على قاعدة تجريبية منفصلة.

### المرحلة C: إصلاح الواجهة

1. حذف `demoUsers` من `js/auth.js`.
2. ضبط `API_CONFIG.baseUrl` على رابط النشر النهائي من إعداد مركزي واحد.
3. توحيد صيغة `GET` و`POST` بين الواجهة والخادم.
4. إظهار حالة الاتصال بوضوح للمستخدم.
5. عدم وضع مفاتيح مزودي الذكاء الاصطناعي في `localStorage` إذا كان الجهاز مشتركًا.
6. منع إدخال HTML غير موثوق في القوالب التي تستخدم `innerHTML`، أو تنظيفه قبل العرض.
7. إضافة اختبار متصفح حقيقي لتسجيل الدخول، إنشاء عميل، إنشاء منتج، إنشاء فاتورة، وتسجيل الخروج.

### المرحلة D: قاعدة البيانات

1. أخذ نسخة احتياطية من الملف الحالي قبل أي تعديل.
2. إنشاء الأوراق المطلوبة عن طريق دالة تهيئة موثوقة.
3. وضع صف العناوين في الصف الأول فقط.
4. إنشاء مستخدم إداري مؤقت عبر مسار آمن، لا عبر كلمة مرور ثابتة في المستودع.
5. إدخال شركة وفرع وحسابات وعملة وإعدادات النظام.
6. اختبار العلاقات بين العملاء والمنتجات والفواتير والحسابات.

### المرحلة E: النسخ الاحتياطي والمراقبة

1. تنفيذ تصدير حقيقي من Sheets إلى ملف XLSX أو CSV/JSON.
2. حفظ النسخ في مجلد Drive مخصص بصلاحيات مقيدة.
3. جدولة النسخ باستخدام Apps Script time-driven trigger أو خدمة جدولة موثوقة.
4. اختبار الاستعادة على نسخة تجريبية، وليس على قاعدة الإنتاج مباشرة.
5. تسجيل كل عملية حساسة في `AuditLogs`.

## 5. طريقة إعداد Google Sheets بالتفصيل

### 5.1 إنشاء قاعدة البيانات

1. افتح [Google Sheets](https://sheets.google.com).
2. أنشئ ملفًا باسم `FINOVATE_DB_PROD` للإنتاج وملفًا آخر باسم `FINOVATE_DB_TEST` للاختبار.
3. لا تستخدم ملف الإنتاج أثناء تطوير الكود.
4. انسخ Spreadsheet ID من الرابط. وهو الجزء الواقع بين `/d/` و`/edit`.

### 5.2 الأوراق المقترحة

ابدأ بالحد الأدنى التالي، ثم أضف بقية الوحدات بعد نجاح الاختبار:

`Users`, `Roles`, `Permissions`, `Companies`, `Branches`, `Customers`, `Suppliers`, `Products`, `Categories`, `Warehouses`, `Stock`, `Sales`, `SalesItems`, `Purchases`, `PurchaseItems`, `Accounts`, `Journal`, `JournalLines`, `AuditLogs`, `Settings`.

يجب أن ينشئ الكود صف العناوين تلقائيًا من مخطط واحد. لا تنشئ عناوين يدوية مختلفة عن `Database.gs`.

### 5.3 صلاحيات ملف Sheets

| الجهة | الصلاحية المقترحة |
|---|---|
| مالك النظام | Owner |
| حساب Apps Script الذي ينفذ Web App | الوصول إلى التعديل |
| المطور | Editor مؤقت أثناء التطوير فقط |
| مستخدمو النظام | لا وصول مباشر إلى ملف Sheets |
| الزوار أو Everyone | ممنوع |
| مجلد النسخ الاحتياطية | مالك النظام + مسؤول احتياطي فقط |

لا تشارك ملف Sheets مع `Anyone with the link`. المستخدمون يجب أن يمروا عبر الواجهة والخادم.

## 6. طريقة إعداد Apps Script ونشره

1. افتح [Google Apps Script](https://script.google.com).
2. أنشئ مشروعًا جديدًا باسم `FINOVATE_ERP_API_PROD`، أو أنشئه من داخل ملف Sheets ليكون مرتبطًا به.
3. أضف ملفات `Code.gs` و`Database.gs` بعد توحيدهما.
4. ضع Spreadsheet ID في إعدادات Script Properties، وليس في واجهة المستخدم.
5. شغّل دالة تهيئة قاعدة البيانات يدويًا لأول مرة.
6. راجع نافذة الصلاحيات وامنح المشروع الوصول إلى Spreadsheet وDrive المطلوبين فقط.
7. أنشئ Deployment جديدًا من نوع **Web app**.
8. اجعل **Execute as** حساب المالك أو حساب خدمة مخصصًا.
9. اجعل الوصول مقيدًا بالمستخدمين المسموحين قدر الإمكان. لا تستخدم `Anyone` إلا بعد إضافة طبقة توثيق حقيقية واختبارها.
10. انسخ رابط `/exec` وضعه في إعدادات الواجهة.
11. اختبر `action=health` أو مسار صحة مخصص قبل اختبار تسجيل الدخول.
12. بعد كل تعديل جوهري، أنشئ إصدارًا جديدًا من Deployment واختبره قبل اعتماده.

الرابط المرفق حاليًا هو [رابط Apps Script الحالي](https://script.google.com/macros/s/AKfycby-3QG6b4aZNo-k9Z4r15YtYUT2TwQhtOBQRBs3qIEb7H8yd4ofq8OwvG25ZipNzosi/exec)، لكنه فشل في الاختبار ويجب عدم توزيعه على المستخدمين قبل إصلاحه.

## 7. طريقة نشر الواجهة

### الخيار الموصى به: GitHub Pages

1. ثبّت التعديلات في المستودع.
2. لا تضع أي مفاتيح سرية أو كلمات مرور في GitHub.
3. اجعل النشر من مجلد الجذر أو من `dist/` حسب إعداد المشروع.
4. فعّل HTTPS.
5. اضبط `API_CONFIG.baseUrl` على Web App الصحيح.
6. اختبر Service Worker بعد النشر على HTTPS.

رابط المستودع: [FINOVATE-ERP-X على GitHub](https://github.com/hacker1998ahmed/FINOVATE-ERP-X).

### الخيار البديل: Netlify أو Google Cloud Storage

يمكن نشر محتويات `dist/` على Netlify أو Google Cloud Storage static website hosting. يجب ألا تضع مجلد Apps Script أو ملفات الأسرار في مجلد النشر العام.

### ما يمكن فعله في Google Drive

يمكن إنشاء مجلد باسم `FINOVATE_ERP_RELEASES` ورفع:

- ملف ZIP للإصدار.
- نسخة من `FINOVATE_DB_TEST`.
- دليل التشغيل.
- ملفات التوثيق.
- النسخ الاحتياطية.

لكن لا تعتمد على فتح `index.html` من Drive ليعمل كتطبيق مستضاف.

## 8. ترتيب الصلاحيات والروابط

### الروابط الأساسية

| الاستخدام | الرابط |
|---|---|
| المستودع البرمجي | [GitHub repository](https://github.com/hacker1998ahmed/FINOVATE-ERP-X) |
| قاعدة البيانات الحالية | [FINOVATE_DB](https://docs.google.com/spreadsheets/d/1Y_1pXGbUMYXJFYne-5uqM3kYgMhDjrVWoej4yII60mM/edit) |
| Apps Script الحالي | [Web App](https://script.google.com/macros/s/AKfycby-3QG6b4aZNo-k9Z4r15YtYUT2TwQhtOBQRBs3qIEb7H8yd4ofq8OwvG25ZipNzosi/exec) |
| إنشاء مشروع Apps Script | [script.google.com](https://script.google.com) |
| إنشاء Google Sheet | [sheets.google.com](https://sheets.google.com) |
| إدارة ملفات Drive | [drive.google.com](https://drive.google.com) |
| Google Sites كخيار صفحات عامة | [Google Sites](https://sites.google.com) |

### نموذج توزيع الحسابات

- حساب المالك: إدارة Apps Script وSheets وDrive والنشر.
- حساب المطور: Editor مؤقت، ويُسحب بعد انتهاء الإصلاح.
- حساب التشغيل: لا يُعطى صلاحية Owner.
- المستخدمون: حسابات تطبيق داخل النظام أو Google OAuth، وليس وصولًا مباشرًا إلى Sheets.
- الزوار: لا وصول إلى قاعدة البيانات أو مجلد النسخ الاحتياطية.

## 9. قائمة اختبار القبول قبل التشغيل

لا تعلن النظام جاهزًا إلا بعد نجاح البنود التالية على بيئة الاختبار:

1. فتح الواجهة عبر HTTPS.
2. تسجيل دخول مستخدم صحيح من قاعدة البيانات.
3. رفض كلمة مرور خاطئة.
4. رفض مستخدم غير نشط.
5. منع المحاسب من تنفيذ عملية ليست ضمن دوره.
6. إنشاء شركة وفرع وعميل ومنتج.
7. إنشاء فاتورة بيع مرتبطة بعميل ومنتج.
8. التأكد من حفظ البيانات في الورقة الصحيحة.
9. إعادة تحميل الصفحة والتأكد من ظهور البيانات المحفوظة.
10. تجربة انقطاع الاتصال ورسالة الخطأ.
11. تشغيل نسخة احتياطية حقيقية.
12. استعادة النسخة على قاعدة اختبار.
13. مراجعة سجل التدقيق.
14. اختبار الهاتف والكمبيوتر.
15. اختبار RTL باللغة العربية وLTR باللغة الإنجليزية.
16. فحص عدم وجود كلمات مرور أو مفاتيح API في ملفات JavaScript العامة.
17. مراجعة سجلات Apps Script وعدم وجود أخطاء.
18. اختبار أكثر من مستخدم في الوقت نفسه.

## 10. قرار الإطلاق المقترح

### الحالة الحالية

**ممنوع الإطلاق التجاري أو فتحه للعامة.** يمكن تشغيل الواجهة محليًا أو على بيئة اختبار غير متصلة ببيانات حقيقية، ويمكن رفع نسخة ZIP إلى Drive للتخزين.

### شروط الانتقال إلى تجربة مغلقة

- إصلاح توافق API.
- إنشاء قاعدة اختبار كاملة.
- إزالة الحسابات التجريبية من الواجهة.
- إضافة توثيق وصلاحيات في الخادم.
- تنفيذ النسخ الاحتياطي والاستعادة فعليًا.
- اجتياز قائمة اختبار القبول.

### شروط الانتقال إلى الإنتاج

- اختبار قبول موثق.
- نسخة احتياطية واستعادة مجربتان.
- حسابات المستخدمين الحقيقية وكلمات مرور آمنة.
- سياسة صلاحيات ومشاركة مكتوبة.
- مراقبة أخطاء Apps Script.
- استضافة HTTPS مستقرة.
- خطة رجوع إلى الإصدار السابق.

## 11. أوامر التشغيل المحلي بعد الإصلاح

```bash
cd FINOVATE-ERP-X
./deploy.sh
cd dist
python3 -m http.server 4173
```

ثم افتح:

```text
http://localhost:4173/login.html
```

هذا التشغيل محلي فقط. لا يعتبر نشرًا إنتاجيًا ولا يحل مشكلة Apps Script أو المصادقة.

## 12. الخلاصة النهائية

**النظام الحالي ليس جاهزًا بالكامل رغم أن ملفات التوثيق تصفه بأنه Production Ready.** الجزء الأمامي موجود ويمكن بناؤه، لكن قاعدة البيانات غير مهيأة، والرابط الخلفي يفشل في الاختبار، والمصادقة تجريبية، والصلاحيات على العميل فقط، والنسخ الاحتياطي محاكاة، وGoogle Drive لا يصلح كاستضافة مباشرة للموقع.

المسار الصحيح هو: **إصلاح الخادم وقاعدة البيانات أولًا، ثم اختبار التكامل، ثم نشر الواجهة على استضافة HTTPS، ثم استخدام Drive للتخزين والنسخ الاحتياطية وليس كخادم للموقع.**

## المراجع

[1]: https://workspaceupdates.googleblog.com/2015/08/deprecating-web-hosting-support-in.html "Deprecating web hosting support in Google Drive"

[2]: https://cloud.google.com/storage/docs/hosting-static-website "Host a static website on Cloud Storage"

[3]: https://workspace.google.com/products/sites/ "Google Sites"

[4]: https://script.google.com/ "Google Apps Script"

[5]: https://sheets.google.com/ "Google Sheets"

[6]: https://drive.google.com/ "Google Drive"

[7]: https://github.com/hacker1998ahmed/FINOVATE-ERP-X "FINOVATE ERP X source repository"

---

**إعداد التقرير:** Manus AI

**ملاحظة أمنية:** يجب تغيير أي كلمة مرور تجريبية أو مفتاح API ظهر في ملفات المشروع أو الرسائل قبل استخدام النظام مع بيانات حقيقية.
